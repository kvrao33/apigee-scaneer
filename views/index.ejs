<!DOCTYPE html>
<html>
<head>
    <title>Data Table Example</title>
    <!-- Required libraries -->
    <link href="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet">   
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js"></script>
    <link rel="stylesheet" href="http://cdn.datatables.net/1.10.2/css/jquery.dataTables.min.css">
    <script type="text/javascript" src="http://cdn.datatables.net/1.10.2/js/jquery.dataTables.min.js"></script>
    <script type="text/javascript" src="http://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="header">
        Apigee Scanner
    </div>
    <div class="container">
        <h2>Proxy Details</h2>
        <div class="table-responsive">
            <table id="myTable" class="table table-striped display" width="100%">
                <thead>
                    <tr>
                        <th>Proxy Name</th>
                        <th>Revisions</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% proxies.forEach((proxy, index) => { %>
                    <tr>
                        <td><%= proxy.name %></td>
                        <td>
                            <input type="hidden" id="proxyName-<%= index %>" value="<%= proxy.name %>">
                            <select id="revision-<%= index %>" class="form-control revision-select">
                                <% proxy.revision.forEach(rev => { %>
                                <option value="<%= rev %>"><%= rev %></option>
                                <% }) %>
                            </select>
                        </td>
                        <td>
                            <button type="button" class="btn btn-success get-report" data-index="<%= index %>">Get Report</button>
                        </td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>
    </div>

    <script>
        $(document).ready(function(){
            $('#myTable').dataTable();

            $('.get-report').click(function(){
                var index = $(this).data('index');
                var proxyName = $('#proxyName-' + index).val();
                var revision = $('#revision-' + index).val();

                console.log('proxyName:', proxyName);
                console.log('revision:', revision);

                if (proxyName && revision) {
                    var url = '/get-report?proxyName=' + encodeURIComponent(proxyName) + '&revision=' + encodeURIComponent(revision);
                    window.open(url, '_blank');
                } else {
                    alert("Please select a revision.");
                }
            });
        });
    </script>
</body>
</html>
